
rocketPos = out.pos;
time = rocketPos(:,1);
altitude = rocketPos(:,3) * -1;
horDist = rocketPos(:,2);

figure
axis equal;
xlabel('Horizontal Position (km)', FontWeight='bold');
ylabel('Altitude (km)', FontWeight='bold');

for i =1:length(time)
    plot(horDistance(1